#include <bits/stdc++.h>
#define mod 1000000007
#define ll long long
using namespace std;

void solve() {
  int N; // Other players
  int K; // Initial position of Chef

  cin >> N >> K;

  for (int i = 0; i < N; i++) {
    int P; // Initial positions of the N players
    cin >> P;
  }


  // Output
  // Print -1 if no player can beat Chef
  // Otherwise, print an integer that refers to the starting
  // square of one player that can beat Chef in the smallest number
  // of turns
}

int main() {
  int T;
  cin >> T;
  while (T--) {
    solve();
  }
  return 0;
}

