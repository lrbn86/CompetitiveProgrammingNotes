#include <bits/stdc++.h>
using namespace std;

void solve() {
  int N; // Number of windows
  cin >> N;
  int threeRoom = 0;
  int fiveRoom = 0;
  int sevenRoom = 0;


  if (threeRoom > 0 && fiveRoom > 0 && sevenRoom
}

int main() {
  int T;
  cin >> T;
  while (T--) {
    solve();
  }
  return 0;
}
