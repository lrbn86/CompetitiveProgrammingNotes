#include <bits/stdc++.h>
using namespace std;

void solve() {
	int n, c0, c1, h;
	string s;
	cin >> n >> c0 >> c1 >> h >> s;
}

int main() {
	int T;
	cin >> T;
	while (T--) {
		solve();
	}
	return 0;
}

