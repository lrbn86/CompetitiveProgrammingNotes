#include <bits/stdc++.h>
using namespace std;

void solve() {
	vector<int> arr;
	int n, k;
	cin >> n >> k;
	for (int i = 0; i < n*k; i++) {
		int a;
		cin >> a;
		arr.push_back(a);
	}

}

int main() {
	int T;
	cin >> T;
	while (T--) {
		solve();
	}
	return 0;
}

