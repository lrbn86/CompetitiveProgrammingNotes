C++

bool isPrime(int n) {
  for (int i = 2; i < n / 2; i++) {
    if (n % i == 0) {
      return false;
    }
  }
  return true;
}

Improved (Use square root instead of division by 2):

bool isPrime(int n) {
  for (int i = 2; i < sqrt(n); i++) {
    if (n % i == 0) {
      return false;
    }
  }
  return true;
}
